# 阶段 8：交接记录（2026-09-08）

> 最后更新：2026-09-08。已完成 8 个 Controller、34 个接口及前端路由的静态盘点，建立 [手动验收对照表](../manual-acceptance.md)；尚未开始阶段 8 的代码调整或完整流程验收。

> 目标：在部署前补齐可复现的手动验收、API 与错误码文档、日志上下文、前端页面状态和启动说明，让其他人能按文档完成注册到交易结束的流程。

原始清单见：[阶段-8-测试质量与文档.md](阶段-8-测试质量与文档.md)。

## 当前结论：下一步做什么

> 最新进展：用户已逐组确认所指导的后端正常/异常验收通过（用户反馈，非助手逐请求取证，不等同全部 69 条）。助手完成七个列表页的状态与重试检查及 390px 窄屏检查，发现移动导航缺失、退出失败误报成功；详见 [前端检查记录](阶段-8-前端检查记录-2026-09-08.md)。下一步优先处理这两项问题，再补其余前端检查。

**接口盘点、热门/健康检查/管理员 ping 的 API 说明和统一错误码表已完成；Postman 集合已整理；下一步在 Postman 导入并配置本地变量，按说明手动验收。**

文档入口：[API 索引](../api/README.md)、[错误码表](../api/error-codes.md)。本次仅补文档并纠正已知描述差异，尚未执行阶段 8 完整回归。

对照表发现健康页与 API 权限不一致、重新启用用户后的旧 Token 约定与代码存在差异，详见 D-03/D-04；先核验并明确期望，再修改业务行为。

建议将对照表放在 `docs/manual-acceptance.md`，每项记录：接口/页面、角色、前置数据、操作、预期 HTTP 状态与业务状态、实际结果、验收日期。先列清单，不把历史验收直接勾成阶段 8 总验收通过。

随后按以下顺序推进：

1. 对照实际接口补齐 `docs/api/`，整理可导入的 Postman 集合和变量说明。
2. 统一错误码、参数错误信息与日志上下文，处理验证码日志遗留问题。
3. 执行正常、异常、权限及订单事务的后端手动验收。
4. 检查前端加载、有数据、空数据、失败四种状态，以及窄屏和主流程。
5. 完善 README 与安全配置示例，按文档重新走完整流程并汇总结果。

## 开始前的项目状态

- 后端：`campus-trade-server`，Spring Boot、MyBatis-Plus、MySQL、Redis；根包 `com.campus.trade.campustradeserver`。
- 前端：`campus-trade-web`，Vue 3 + TypeScript；继续在同一项目中完善学生端与管理员端。
- 阶段 1–6 已实现认证、商品、收藏、订单和管理员功能，并有各阶段手动验收记录；阶段 8 仍需完整回归。
- 阶段 7 的分类缓存、热门商品缓存、失效与前端热门区域已完成开发和主要验收。
- Redis 超时已设为连接 1 秒、命令 1 秒。用户重启后的 8080 实测：匿名分类/热门约 2 秒降级成功，认证 Redis 故障约 1 秒返回 503，恢复后接口正常。
- 连接拒绝和黑名单读取成功、写入失败已在独立临时实例补验通过；临时实例已清理。详见 [阶段 7 Redis 故障验收](阶段-7-Redis故障验收-2026-09-08.md)。
- 阶段 7 的代码与文档仍有未提交修改，后续必须保留；暂存和提交由用户自行完成。

## 本次文档盘点

- `docs/api/` 已有阶段 1、3、4、5、6 文档；覆盖是否完整、是否符合当前代码尚待逐项核对。
- 阶段 1 API 文件名为 `docs/api/  phase-1.md`，带两个前导空格；后续整理时检查引用后再规范命名。
- 已有 `docs/cache.md` 与分阶段学习、验收记录。
- 根目录 `README.md`、`.env.example` 仍待补；已新增 [Postman 集合与使用说明](../postman/README.md)，69 条请求覆盖 34 个接口，尚未实际导入或运行。
- 不将文档目录存在等同于内容完整或验收通过。

## 协作与执行约定

- 默认仍采用学习式协作：说明目的、完整文件路径、操作步骤、完整代码、关键含义与验收方式；用户明确委托时，助手可以直接修改代码。
- 后端接口以 Postman/Apifox 或真实服务请求验收；前端用浏览器，可使用 DevTools MCP。请求脚本的实测结果不能写成已完成 Postman 导入验收。
- 本阶段不新建 `src/test`、MockMvc 或前端 `.spec` 测试，不主动运行自动测试或格式化。
- 原始阶段 8 清单包含前端构建，但用户此前“不主动运行前端构建”的约定继续有效：先保留为待办，获得后续明确指示后，按项目声明的 Node 版本执行；未执行时如实记录。
- 用户提供的本地账号和密码不写入交接文档、Postman 导出文件或提交内容。集合使用空值变量，凭据由使用者在本地填写。
- 不自动暂存、提交 Git，不覆盖已有未提交工作。

## 阶段 7 转入的日志问题

`EmailCodeService` 的开发邮件模式仍输出明文验证码，`docs/cache.md` 保留了开发例外说明；阶段 8 原始要求明确禁止日志记录验证码。此项作为阶段 8 的日志整改任务，不能直接勾选完成。

整改时同时明确本地如何取得验证码并完成注册验收，避免只删除日志后导致注册流程无法复现。密码、JWT、SMTP 授权码与完整 Base64 图片也不得记录到日志。

## 阶段 8 验收清单

### 接口与手动验收材料

- [x] 建立接口/页面与手动验收对照表（静态盘点完成，运行用例均待验收）。
- [ ] 所有接口补齐请求、响应、权限和错误示例。
- [x] 建立错误码表，区分 HTTP 状态与响应业务码，包含 401、403、503 的实际语义；未统一的框架异常和业务码 401 边界已标注。
- [x] 生成不含真实凭据的 `docs/postman/校园二手交易平台.postman_collection.json`，完成静态结构与覆盖检查。
- [ ] 在 Postman 验证导入和本地变量配置，并执行请求。
- [ ] 覆盖注册、登录、退出、商品归属、分类状态、收藏唯一性、订单全部状态、管理员权限、Redis 失效。
- [ ] 覆盖非法分页、不存在 ID、参数错误、非法 Base64、禁用用户、唯一键重复。
- [ ] 验证下单/取消时商品状态与订单记录同时正确，记录前后状态。

### 错误与日志

- [ ] 参数错误信息能定位字段，前端不收到堆栈或敏感数据。
- [ ] 增加请求 ID 或统一日志上下文，并验证请求与日志可对应。
- [ ] 解决开发验证码日志问题，记录可用的本地注册验收方式。
- [ ] 检查敏感信息日志边界，保留必要的操作和故障信息。

### 前端与最终文档

- [ ] 每个数据页检查加载、有数据、空数据、请求失败状态。
- [ ] 浏览器回归认证拦截、搜索分页、收藏变化、订单按钮和管理员权限。
- [ ] 检查登录、注册、商品卡/表单、订单列表、管理员表格的窄屏表现。
- [ ] 前端构建：依照用户后续指示执行，当前未运行。
- [ ] 完善根 README：简介、架构、环境、配置、迁移、启动、Postman 导入、构建与演示流程。
- [ ] 提供 `.env.example`，说明变量如何被实际启动方式读取，不假设 Spring Boot 自动加载该文件。
- [ ] 编写双用户交易及管理员操作的完整手动验收脚本，并按 README 重新执行。
- [ ] 检查 Git 待提交内容不含密钥、真实凭据、数据库备份、依赖或构建产物。
- [ ] 更新最终交接记录，由用户自行暂存、提交。

## 阶段边界与本次完成项

- 本阶段做质量、可复现验收与文档完善；Docker、Compose、Nginx 部署属于阶段 9。
- 本次创建交接文档并完成接口/页面对照表；未修改业务代码，未重新执行业务验收、构建、格式化或自动测试。
- 下次从导入 Postman 集合与配置本地变量开始，按使用说明逐条发送请求；完整文档字段核对与运行验收仍待进行。

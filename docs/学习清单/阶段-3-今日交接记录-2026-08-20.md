# 阶段 3：交接记录（更新于 2026-08-27）

> 阶段 3 的分类与商品后端已完成：分类管理、发布、编辑、下架、公开分页查询、公开详情和我的发布均已实现并完成本轮 Postman 验收。前端项目使用 TypeScript，下一步进入前端商品功能。下次继续时，直接对 Codex 说：`继续阶段 3，从前端商品 API 模块 product.ts 开始；我自己手动写代码，你一次告诉我一个文件该写什么，并解释代码。`

## 前置阶段已完成

### 阶段 1：认证后端

- [x] 邮箱验证码、注册、登录、JWT 鉴权、当前用户查询与退出 Token 黑名单。
- [x] Spring Security 路由保护与管理员角色校验。
- [x] MySQL、Redis、Flyway 和本机敏感配置已就绪。

### 阶段 2：认证前端

- [x] Vue 登录、注册、个人中心、403 和 404 页面。
- [x] Pinia 认证状态、localStorage 会话恢复、Axios Token 注入与 401 自动清理。
- [x] 路由登录保护、管理员角色保护和学生端菜单。
- [x] 注册验证码倒计时、完整认证链路和前端构建验收均已由用户确认通过。

## 阶段 3 目标

完成“分类与商品”闭环：分类管理接口、商品发布/编辑/下架、商品广场、搜索筛选分页、商品详情、我的发布，以及单张 Base64 图片上传与前后端校验。

本阶段的详细任务清单在：[阶段-3-分类与商品.md](阶段-3-分类与商品.md)。

## 当前起点

- 后端目录：`campus-trade-server`
- 前端目录：`campus-trade-web`
- 后端根包：`com.campus.trade.campustradeserver`
- 数据库：`campus_trade`（MySQL `localhost:3306`）
- Redis：`localhost:6379`
- 后端端口：`8080`
- 已执行的 Flyway 迁移：`V1__create_sys_user.sql`、`V2__create_category.sql`、`V3__create_product.sql`、`V4__seed_categories.sql`。
- 已确认：`category` 与 `product` 表已创建；`category` 表已有 5 条启用初始分类。
- 已执行的 Flyway 迁移不可修改；今后表结构变化必须使用新的版本迁移。

## 本次已完成

### 接口文档与数据库

- [x] 已创建 [docs/api/phase-3.md](../api/phase-3.md)，定义分类、商品、权限、状态、图片与分页契约。
- [x] 已创建并启动执行 `V2__create_category.sql`：分类表、分类名唯一索引、状态/排序查询索引。
- [x] 已创建并启动执行 `V3__create_product.sql`：商品表、状态/时间、分类、卖家查询索引。
- [x] 已创建并启动执行 `V4__seed_categories.sql`：教材书籍、数码电器、生活用品、运动器材、服饰鞋包，均为 `ENABLED`。

### 分类后端基础结构

- [x] 已创建 `Category` 实体、`CategoryMapper`、`CategoryService` 与 `CategoryStatus` 枚举。
- [x] `CategoryStatus` 统一管理 `ENABLED`、`DISABLED`；数据库仍保存对应字符串，业务代码通过 `CategoryStatus.xxx.name()` 使用。
- [x] 已创建公开 `CategoryResponse` 与管理员 `AdminCategoryResponse`；两者分开避免把管理员字段暴露给公开接口。

### 公开分类接口

- [x] 已实现 `GET /api/categories`：只查询 `ENABLED` 分类，按 `sort`、`id` 升序。
- [x] 已在 `SecurityConfig` 精确放行 `GET /api/categories`；其他分类写操作不会因此自动公开。
- [x] 已手动确认公开分类接口可返回分类数据。

### 管理员分类接口

- [x] 已实现 `POST /api/admin/categories`：名称去首尾空格、名称唯一校验、后端固定创建 `ENABLED` 状态。
- [x] 已手动验收管理员新增分类；管理员接口由既有 `/api/admin/**` 规则保护。
- [x] 已实现 `GET /api/admin/categories`：返回启用和停用的全部分类，按 `sort`、`id` 升序。
- [x] 已手动验收管理员查询全部分类。
- [x] 已创建 `CategoryUpdateRequest`，支持只传 `name` 或只传 `sort`，并用 `@AssertTrue` 限制两者不能同时缺失。
- [x] 已实现 `PATCH /api/admin/categories/{id}` 的 Service 和 Controller：分类不存在返回 `2002`；改名时会排除自身后查重，重复返回 `2001`；未传字段保持原值。
- [x] 已手动验收 `PATCH /api/admin/categories/{id}`：管理员修改名称/排序及其参数、重复名称、分类不存在与权限边界均已确认。
- [x] 已创建 `CategoryStatusUpdateRequest`：使用 `CategoryStatus` 枚举接收 `ENABLED`、`DISABLED`，并用 `@NotNull` 拒绝空状态。
- [x] 已实现 `PATCH /api/admin/categories/{id}/status`：Service 先校验分类存在（不存在返回 `2002`），再将枚举名称写入数据库并返回完整分类数据。
- [x] 已用管理员 Token 验证状态更新接口的成功路径；请求方法必须是 `PATCH`，不是 `GET`。
- [x] 已完整手动验收 `PATCH /api/admin/categories/{id}/status` 的公开列表联动、请求体/枚举异常、分类不存在和权限边界。

### 全局异常处理

- [x] `GlobalExceptionHandler` 已新增 `HttpMessageNotReadableException` 处理器，目标是缺少请求体或 JSON 格式错误时返回 HTTP 400 和统一 `ApiResponse` JSON。
- [x] 已复验修改分类接口的请求体与参数异常处理；不传 body 或 JSON 格式错误返回统一 HTTP 400 JSON。

### 商品发布后端（已完成并进行基础手动验收）

- [x] 已创建 `Product` 实体、`ProductMapper`、`ProductStatus` 与 `CreateProductRequest`。
- [x] `ProductStatus` 统一管理 `ON_SALE`、`LOCKED`、`SOLD`、`OFF_SHELF`；`Product.status` 仍保存字符串，通过 `ProductStatus.xxx.name()` 写入。
- [x] `CreateProductRequest` 只接收 `categoryId`、`title`、`description`、`price`、`imageBase64`；用 Bean Validation 约束分类 ID、标题、描述与 `BigDecimal` 价格，并忽略未知 JSON 字段，避免客户端影响卖家、状态或浏览量。
- [x] 已创建 `ProductService.createProduct(Long sellerId, CreateProductRequest request)`：校验分类存在且未停用（失败返回 `2002`），卖家 ID 只取服务端参数，标题/描述去首尾空格，状态固定为 `ON_SALE`、浏览量固定为 `0`，插入后按生成 ID 查询完整记录。
- [x] 已创建 `ProductImageValidator`，并在保存商品前调用：允许 `null` 图片；非空时校验 JPEG/PNG/WebP Data URL、Base64、编码及解码后 2 MB 上限、实际 ImageIO 解码和 MIME/真实格式一致性；失败统一抛 `3004`。
- [x] 已在 `pom.xml` 加入 TwelveMonkeys `imageio-webp:3.12.0`，用于 WebP 实际解码。
- [x] 已创建 `ProductDetailResponse`：返回商品、分类和卖家展示字段；`status` 使用 `ProductStatus` 枚举，不暴露卖家邮箱、密码或其他实体敏感字段。
- [x] 已创建 `ProductController` 并实现 `POST /api/products`：从 `@AuthenticationPrincipal AuthenticatedUser` 取得卖家 ID，使用 `@Valid` 校验请求，调用 `ProductService.createProduct`，再以 `ApiResponse<ProductDetailResponse>` 返回。
- [x] 已用 Postman 手动验证正常发布：含 JPEG 图片和 `imageBase64: null` 两条成功路径均返回 `code: 0`、`status: ON_SALE`、`viewCount: 0`，并正确返回分类名和当前卖家展示名。
- [x] 已用 Postman 验证客户端伪造 `sellerId`、`sellerName`、`status`、`viewCount` 会被忽略，实际返回仍为当前用户、`ON_SALE` 和 `0`。
- [x] 已用 Postman 验证未登录发布返回 `code: 401`；不存在分类返回 `code: 2002`；价格为 `0` 返回统一 `code: 400`。
- [x] 已用 Postman 验证非图片 Base64、伪造 JPEG MIME 与 JPEG 内容伪装成 PNG 均返回 `code: 3004`。
- [ ] 本轮尚未手动覆盖：原始图片超过 2 MB、有效 WebP 图片、停用分类发布、标题/描述边界。后续做完整商品接口验收时一并补测。

### 商品编辑与主动下架后端（已完成并完成核心手动验收）

- [x] 已创建 `UpdateProductRequest`：字段和发布请求一致，使用相同的分类、标题、描述和价格校验；通过 `@JsonIgnoreProperties(ignoreUnknown = true)` 忽略客户端伪造的额外字段。
- [x] 已在 `ProductService` 实现商品操作前置校验：商品不存在返回 `3001`；非卖家抛出 Spring Security 的 `AccessDeniedException` 并由既有处理器返回 HTTP/响应体 `403`；非 `ON_SALE` 状态返回 `3003`。
- [x] 已实现完整编辑：复用启用分类和图片校验，只更新分类、标题、描述、价格和图片；不修改卖家、状态、浏览量或创建时间。
- [x] 已实现主动下架：仅允许卖家本人将 `ON_SALE` 商品改为 `OFF_SHELF`。
- [x] 已在 `ProductController` 实现 `PUT /api/products/{id}` 与 `POST /api/products/{id}/off-shelf`，并校验商品 ID 必须为正整数。
- [x] 已用 Postman 创建用于验收的无图商品，并验证：卖家本人编辑成功；另一普通用户编辑返回“权限不足”（HTTP/响应体 `403`）；卖家本人下架成功；下架后再次编辑或下架返回状态限制错误。

### 商品广场分页查询（已完成并通过手动验收）

- [x] 已创建 `ProductQuery`：默认 `page=1`、`pageSize=12`，并校验页码、每页上限、分类 ID 和关键词长度。
- [x] 已创建 `ProductPageResponse`：用于商品卡片，不含详情描述和更新时间。
- [x] 已创建通用 `PageResponse<T>`：统一向前端返回 `page`、`pageSize`、`total` 和 `records`。
- [x] 已在 `pom.xml` 加入 `mybatis-plus-jsqlparser:3.5.14`；MyBatis-Plus 3.5.14 的 `PaginationInnerInterceptor` 位于该扩展依赖中。
- [x] 已创建 `MybatisPlusConfig`，注册 MySQL 方言的 `PaginationInnerInterceptor`。
- [x] 已在 `ProductMapper` 新增 `selectOnSalePage`：使用 `Page<ProductPageResponse>`/`IPage<ProductPageResponse>`，联查 `product`、`category`、`sys_user`，固定只查 `ON_SALE`，支持参数化分类和标题/描述关键词筛选，并按创建时间、ID 倒序。
- [x] 已在 `ProductService` 创建 `Page`、调用 Mapper，并将 `IPage<ProductPageResponse>` 转换为项目自己的 `PageResponse<ProductPageResponse>`；空白关键词按未传处理。
- [x] 已在 `ProductController` 增加公开 `GET /api/products`，并在 `SecurityConfig` 精确放行该 GET 路由，不放行商品写操作。
- [x] 已用 Postman 验收公开访问、分页、分类筛选、关键词、组合筛选、空白关键词、非法分页参数和下架商品不出现在公开列表。

### 商品公开详情与浏览量（已完成并通过手动验收）

- [x] 已在 `ProductMapper` 实现“仅对在售商品浏览量加一”的条件更新，并在成功后联查商品、分类和卖家完整详情。
- [x] 已在 `ProductService` 实现公开详情：条件更新未命中或详情查询为空时返回 `3001`；成功响应返回本次访问后增加的 `viewCount`。
- [x] 已在 `ProductController` 实现 `GET /api/products/{id}`；路径 ID 必须为正整数。
- [x] 已在 `SecurityConfig` 用正则请求匹配器仅放行数字详情路径，避免错误公开后续的 `/api/products/mine`；零和负数 ID 仍由 Controller 返回 `400`。
- [x] 已用 Postman 验收未登录访问在售详情、连续访问浏览量递增、下架或不存在商品返回 `3001`，以及零/负数 ID 返回 `400`。

### 我的发布后端（已完成并通过手动验收）

- [x] 已创建 `MyProductQuery`：默认分页、可选关键词、可选状态；状态仅允许 `ON_SALE`、`LOCKED`、`SOLD`、`OFF_SHELF`。
- [x] 已扩展 [docs/api/phase-3.md](../api/phase-3.md) 的“我的发布”契约：支持关键词模糊搜索和状态筛选，未传状态时包含当前用户的全部商品状态。
- [x] 已在 `ProductMapper` 实现当前卖家分页联表查询：卖家 ID 只来自服务端，关键词和状态均使用参数化 SQL。
- [x] 已在 `ProductService` 将当前卖家查询结果转换为 `PageResponse<ProductPageResponse>`；关键词空白时按未传处理。
- [x] 已在 `ProductController` 实现 `GET /api/products/mine`，从 `@AuthenticationPrincipal` 取得当前用户 ID；不增加公开规则，继续要求登录。
- [x] 已用 Postman 验收默认全部状态、关键词、状态、组合筛选、非法状态、未登录 `401` 以及账号间数据隔离。

## 接下来怎么继续

1. 在 `campus-trade-web/src/api/` 新建 `product.ts`，封装商品广场、详情、发布、编辑、下架和我的发布请求。
2. 创建商品广场页面：分类筛选、关键词、分页、商品卡片、图片占位以及加载/空/错误状态。
3. 再依次实现商品详情、发布/编辑表单和我的发布页面，并接入菜单与路由守卫。
4. 最后以浏览器手动发布、编辑、下架和跨账号浏览商品，运行前端构建并记录阶段状态。

下次开始可直接说：

```text
继续阶段 3。从前端商品 API 模块 product.ts 开始；我自己手动写代码，你一次告诉我一个文件该写什么，并解释代码。
```

## 重要约束

- 已执行的 Flyway 迁移不可修改；任何表结构变化都新增下一版本迁移。
- 公开商品列表和详情只展示 `ON_SALE` 商品；下架商品不能继续出现在公开列表。
- 只有商品卖家本人、且商品处于 `ON_SALE`，才能编辑或主动下架；`LOCKED` 商品不能在本阶段下架。
- 搜索必须使用参数化 SQL，不能拼接关键词。
- 前端图片校验只是体验优化，后端必须独立完成 MIME、实际图片格式和 2 MB 大小校验。
- 不要在代码、文档、截图、Git 或接口导出中记录真实密码、Token、验证码、数据库凭据或 JWT 密钥。

## 当前工作区注意事项

- 商品相关代码现包含 `UpdateProductRequest.java`、`ProductQuery.java`、`MyProductQuery.java`、`ProductPageResponse.java`、`ProductDetailResponse.java`、`ProductMapper.java`、`ProductService.java`、`ProductController.java`；公共配置新增 `MybatisPlusConfig.java` 和 `PageResponse.java`，`pom.xml` 新增 MyBatis-Plus 分页解析依赖。提交前在 IDE 检查改动，确认不遗漏或混入无关内容。
- 本次新增学习文档包括 `docs/superpowers/specs/2026-08-26-product-editing-and-off-shelf-design.md`、`docs/superpowers/plans/2026-08-26-product-editing-and-off-shelf.md`、`docs/superpowers/specs/2026-08-26-product-public-pagination-design.md`、`docs/superpowers/plans/2026-08-26-product-public-pagination.md`、`docs/superpowers/specs/2026-08-27-product-public-detail-design.md`、`docs/superpowers/plans/2026-08-27-product-public-detail.md`、`docs/superpowers/specs/2026-08-27-my-products-search-and-status-design.md`、`docs/superpowers/plans/2026-08-27-my-products-search-and-status.md`；本交接记录和 `docs/api/phase-3.md` 也有未提交修改。
- 已执行的 Flyway 迁移仍不可修改；本次没有修改迁移 SQL。
- 阶段 2 的历史交接文件中记录过命令行 Node 版本不匹配；但 `阶段-2-手动验收完成记录-2026-08-19.md` 已确认前端构建验收通过。如再次运行前端命令失败，先检查 `campus-trade-web/.nvmrc` 要求的 Node `20.19.5`。

## 阶段 3 完成标准

- [x] 分类、商品数据库迁移与 API 文档完成。
- [x] 后端商品接口已完成本轮手动验收：分类、发布、商品所有权、主动下架、公开查询、公开详情、浏览量和我的发布均已验证；图片大小/WebP、停用分类发布和文本边界仍待补充验收。
- [ ] 商品广场、详情、发布/编辑和我的发布页面可用。
- [ ] 商品发布、筛选搜索分页、跨账号权限、下架、图片校验均完成验收。
- [ ] 前端构建通过，并记录本阶段交接状态。

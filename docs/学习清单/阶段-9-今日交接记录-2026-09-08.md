# 阶段 9：交接记录（2026-09-08）

> 后续进度见 [2026-09-09 交接记录](阶段-9-今日交接记录-2026-09-09.md)，本文件保留当日历史状态。

> 状态：已进入部署阶段，完成原始清单与实际代码的初步对照；尚未创建镜像、启动容器或部署服务器。用户反馈阶段 8 收尾完成，保留未运行前端构建及两项行为约定未明确的记录。

原始要求：[Docker 与 Nginx 部署](阶段-9-Docker与Nginx部署.md)。

## 目标与第一步

使用四个服务 `mysql`、`redis`、`server`、`nginx`，由 Nginx 同时提供 Vue 页面和 `/api` 代理；数据库与 Redis 只走内部网络，数据保存在命名卷。

**第一步：确定本机 Docker 验证或 Linux 目标环境，再建立部署配置。** 本机已发现 Docker CLI 20.10.16、Compose v2.6.0，CLI 可用不等于 Docker daemon 或镜像构建可用；守护进程连接仍待确认。

## 与实际代码必须对齐的事项

- 前端 Node 固定为 `.nvmrc` 的 20.19.5，镜像构建必须使用一致版本。
- Java 编译目标 17，后端 Maven Wrapper 为 3.9.16。
- JWT 实际配置为 `app.jwt.secret`，可由 `APP_JWT_SECRET` 提供；旧计划中的 JWT_SECRET 不能未经映射直接使用。
- `/api/health` 当前落入 authenticated。部署前需明确是否将这个固定 UP 的存活探测公开；不将它当数据库/Redis 就绪检查。
- 当前验证码仅存 Redis，无真实邮件发送；MAIL_MODE=smtp 是旧计划中的目标而非已有实现。正式远程注册前需实现并验证 SMTP，不得靠环境变量伪装已完成。
- 用户重新启用后旧 Token 行为仍有待确认项；部署配置不会自动改变它。
- `application-local.yml` 含本机私有配置，必须通过 `.dockerignore` 排除，不得打入构建上下文或 JAR。

## 实施顺序

1. 新建 `deploy/README.md`、`deploy/.env.example`，明确部署模式、变量与未实现前置事项。
2. 后端多阶段 Dockerfile：Maven 构建、JRE 非 root 运行，配置通过环境传入。
3. 前端多阶段 Dockerfile：Node 20.19.5 构建，Nginx 提供 dist；设置 history fallback、静态缓存和 API 代理。
4. Compose 编排四服务：只暴露 Nginx 端口，mysql_data/redis_data 命名卷，Redis 密码、依赖健康条件、restart 策略。
5. 静态校验配置；确认现有“不主动构建”约定后再执行 Docker build（其中会运行前端构建），不自动运行测试。
6. 本机或 Linux 运行验收：页面、登录/权限、同源 API、容器重建数据保留、日志、未知页面与未知 API。

## 文件清单

- [ ] `campus-trade-server/Dockerfile` 与 `.dockerignore`
- [ ] `campus-trade-web/Dockerfile` 与 `.dockerignore`（具体构建上下文确定后安排忽略文件）
- [ ] `deploy/docker-compose.yml`
- [ ] `deploy/nginx.conf`
- [ ] `deploy/.env.example`（占位值；实际 deploy/.env 忽略）
- [ ] `deploy/README.md`（启动、停止、更新、备份、恢复与验收）
- [ ] 健康检查与 SMTP 前置事项已明确/实现并验收
- [ ] 配置检查、镜像构建和运行验收
- [ ] 用户自行暂存、提交；助手不改变 Git index 或提交历史

## 协作与边界

继续保留阶段 8 未提交修改。默认按学习式步骤推进，解释文件位置、内容、目的与验收方式；用户明确委托时助手可以代写。无服务器地址不尝试远程部署，不记录真实密码/密钥/SMTP 授权码；不执行删除数据卷的 down -v。

本次只启动阶段九交接与环境盘点，不将计划文件的存在标记为部署完成。

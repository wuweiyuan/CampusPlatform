# 阶段 5：订单状态机与事务

## 目标

实现单件商品交易：下单、取消、模拟付款、确认完成。核心是状态机和事务，确保同一商品只有一个买家成功下单。

## 本阶段文件地图

后端从根包 `com.campus.trade.campustradeserver` 新建：

```text
order/entity/Order.java
order/mapper/OrderMapper.java
order/service/OrderService.java
order/controller/OrderController.java
order/dto/CreateOrderRequest.java
order/vo/OrderDetailResponse.java
order/vo/OrderPageResponse.java
```

SQL 文件：`campus-trade-server/src/main/resources/db/migration/V6__create_orders.sql`。接口不使用 MockMvc 测试；如果你想练习 JUnit，可为纯状态规则创建 `campus-trade-server/src/test/java/com/campus/trade/campustradeserver/order/service/OrderStatusRuleTest.java`，只传入状态并断言允许/拒绝，不发 HTTP 请求、不依赖 Controller。前端文件：`campus-trade-web/src/api/order.js`、`campus-trade-web/src/views/order/MyOrdersView.vue`、可选 `OrderDetailView.vue`；商品详情 `src/views/market/ProductDetailView.vue` 在本阶段修改并增加购买按钮。

## 1. 先固定状态表

| 操作 | 谁能操作 | 订单当前状态 | 商品当前状态 | 订单结果 | 商品结果 |
|---|---|---|---|---|---|
| 创建订单 | 买家 | 无 | `ON_SALE` | `PENDING_PAYMENT` | `LOCKED` |
| 取消订单 | 买家 | `PENDING_PAYMENT` | `LOCKED` | `CANCELLED` | `ON_SALE` |
| 模拟付款 | 买家 | `PENDING_PAYMENT` | `LOCKED` | `PAID` | `SOLD` |
| 确认完成 | 买家 | `PAID` | `SOLD` | `COMPLETED` | `SOLD` |

- [ ] 把此表、接口、权限写到 `docs/api/phase-5.md`。
- [ ] 明确：用户不能买自己的商品；卖家不能代替买家取消/付款/完成；前端不得传价格、卖家、状态、时间。

## 2. 订单表

- [ ] 创建 `orders` 表：`id`、唯一 `order_no`、`buyer_id`、`seller_id`、`product_id`、`amount`、`status`、`created_at`、`paid_at`、`completed_at`、`updated_at`。
- [ ] 对买家订单、卖家订单、商品 ID、订单状态、订单号建立索引。
- [ ] 订单号只能由后端生成，格式写入文档，数据库唯一约束兜底。

## 3. 后端事务

- [ ] 接口：创建订单、订单详情、我的买入订单、我的卖出订单、取消、模拟付款、确认完成。
- [ ] 先写一张手动验收表：两个不同买家对同一商品下单时，预期只有一个成功，商品最终为 `LOCKED`。功能完成后在两个 Postman 窗口尽量同时发送请求，记录结果；这只是学习验证，不要求编写并发自动化测试。
- [ ] 创建订单必须 `@Transactional`：查询/原子更新商品仅允许 `ON_SALE → LOCKED`，检查不能买自己，插入订单快照（当前价格、卖家）。条件更新没有更新到行时表示已被别人抢走，返回业务错误。
- [ ] 取消也必须事务：确认调用者是买家，订单只能 `PENDING_PAYMENT → CANCELLED`，商品 `LOCKED → ON_SALE`。
- [ ] 付款也必须事务：确认买家，订单只能 `PENDING_PAYMENT → PAID`，记录 `paid_at`，商品 `LOCKED → SOLD`。
- [ ] 完成也必须事务：确认买家，订单只能 `PAID → COMPLETED`，记录 `completed_at`，商品仍是 `SOLD`。
- [ ] 每次变更都从数据库读取当前状态；重复支付、越级完成、重复取消都必须报错，不能静默成功。

## 4. 用 Postman 手动验收

- [ ] 用 Postman 验证正常流程：下单→取消，商品回到在售。
- [ ] 用 Postman 验证正常流程：下单→付款→完成，商品为已售。
- [ ] 验证不能购买自己、不能购买下架/锁定/已售商品。
- [ ] 用第三个用户 Token 验证不能取消/付款/完成别人的订单。
- [ ] 手动按错误顺序操作，确认都失败；尝试两个买家同时下单，确认只有一个成功。
- [ ] 验证买入和卖出列表只返回当前用户相关数据，且分页正确。

## 5. Vue 页面

- [ ] 商品详情页只为“非本人、在售商品”显示购买按钮；点击弹窗显示后端商品名和价格后确认下单。
- [ ] 创建成功跳到订单详情或我的订单。
- [ ] 我的订单包含“买入/卖出”页签、分页、状态标签、商品链接；待付款显示取消/付款，已付款显示确认完成。
- [ ] 取消和付款必须二次确认；成功后重新从服务器加载数据。
- [ ] 后端提示状态冲突时显示错误并刷新订单/商品；在浏览器手动检查不同状态下按钮显隐。
- [ ] 买家卖家完整手工流程通过、`npm run build` 通过后提交 Git。

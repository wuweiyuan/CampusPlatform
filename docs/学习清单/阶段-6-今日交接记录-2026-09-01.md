# 阶段 6：交接记录（2026-09-01）

> 目标：在同一个 Vue 项目中提供管理员分类、用户、商品和订单管理。所有管理员能力必须由后端验证 `ADMIN` 角色；前端菜单和路由守卫只负责体验，不能替代后端权限控制。

## 当前结论：下一步做什么

阶段 6 范围较大，按以下四个小节依序完成，避免把权限、数据与页面一次性混在一起：

1. **管理员身份与统一权限保护**：准备本地管理员账号；集中限制 `/api/admin/**` 为 `ADMIN`；确认普通 `USER` Token 一律返回 `403`。
2. **分类管理**：管理员查看全部分类（含停用）、新增、编辑名称/排序、启用/停用；已停用分类必须使商品发布/编辑在后端失败。
3. **用户管理**：分页筛选、启用/禁用；禁止管理员禁用自己；每次 JWT 请求检查用户状态，使禁用用户的旧 Token 立即失效。
4. **商品、订单与前端管理台**：管理员商品筛选与仅对在售商品下架；订单只读筛选；接入四个管理页面、动态菜单与路由守卫。

“管理员身份与统一权限保护”和“分类管理”均已完成：已有本地管理员账号；管理员 Token 请求 `/api/admin/categories` 返回 200，普通 `USER` Token 返回 403；分类接口、停用分类后的商品限制与分类管理页面均已手动验收。

当前处于**用户管理后端的中间阶段**。用户亲自完成了枚举化与用户列表查询的代码输入；状态更新方法、Controller、异常响应统一处理和手动验收尚未完成。前端尚未开始；后续仍由用户亲自编写，我按步骤讲解并检查。

阶段 6 原始需求见：[阶段-6-管理员后台.md](阶段-6-管理员后台.md)。

## 开始前的项目状态

- 后端：`campus-trade-server`，根包为 `com.campus.trade.campustradeserver`；阶段 6 推荐新建 `admin/controller` 与 `admin/service`，复用已有分类、用户、商品、订单的 Entity/Mapper，不新建重复表。
- 前端：`campus-trade-web`，Vue 3 + TypeScript；已有路由守卫，会读取 `meta.requiresAuth` 和 `meta.roles`，管理员路由可沿用 `roles: ["ADMIN"]`。
- 认证：JWT 中已有用户身份与角色；公开注册只能创建 `USER`，本地管理员账号必须单独创建，密码保持 BCrypt 哈希。
- 阶段 5：订单后端、分页接口与前端交易闭环已实现；交接记录已更新。工作区仍有阶段 5 的未提交改动，开始阶段 6 时必须保留，不能覆盖或混入无关修改。
- 当前用户决定：不主动运行前端构建、格式化或自动测试；后端与前端以 Postman、Apifox、浏览器进行手动验收，并在最终记录中如实说明未执行的命令。
- 协作方式：用户手动编写代码；助手先说明为什么做、完整文件路径、应写的完整代码和关键代码含义，再检查用户写入结果。除非用户明确要求，不直接代写业务代码。

## 阶段边界

- 管理员可以管理分类、用户、商品和查看订单，但本阶段不新增第二个前端项目，也不新增第二套用户、商品、订单表。
- 管理员订单接口只读：不能替用户付款、取消或确认完成订单。
- 管理员只能下架 `ON_SALE` 商品；`LOCKED` 商品有待付款买家，本阶段不强制下架。
- 用户列表绝不返回密码哈希、JWT 或其他敏感认证数据。
- 管理员不能禁用当前登录的管理员账号。
- 用户被禁用后，推荐每次 JWT 请求读取用户状态并拒绝该请求；这样旧 Token 立即失效。

## 已有能力与待补能力

| 能力 | 当前状态 | 阶段 6 要做 |
| --- | --- | --- |
| JWT 登录与角色 | 已有 | 集中限制管理员 API，并验证 `USER` 返回 403。 |
| 分类 | 已有普通业务分类 | 管理员完整 CRUD、启用/停用、停用后拒绝商品提交。 |
| 用户 | 已有认证用户 | 管理员分页筛选、启用/禁用、禁用后 Token 失效。 |
| 商品 | 已有发布、编辑、下架 | 管理员跨卖家筛选，仅下架在售商品。 |
| 订单 | 已有交易状态机 | 管理员只读分页与筛选，不改变订单状态。 |
| 前端 | 已有学生侧栏与路由守卫 | 管理员菜单、四个管理页、403 和加载/空/错误状态。 |

## 建议文件地图

```text
campus-trade-server/src/main/java/com/campus/trade/campustradeserver/
└── admin/
    ├── controller/
    │   ├── AdminCategoryController.java
    │   ├── AdminUserController.java
    │   ├── AdminProductController.java
    │   └── AdminOrderController.java
    └── service/
        ├── AdminCategoryService.java
        ├── AdminUserService.java
        ├── AdminProductService.java
        └── AdminOrderService.java

campus-trade-web/src/views/admin/
├── CategoryManageView.vue
├── UserManageView.vue
├── ProductManageView.vue
└── OrderManageView.vue
```

实际 DTO、VO、Mapper 方法应放在已有业务模块或 `admin` 模块中，按实现时的职责划分决定；不要为了凑文件地图创建空类。

## 每小节的固定验收

1. 先在 `docs/api/` 写清接口、参数、响应、权限和错误情况。
2. 先用普通 `USER` Token 请求每一个 `/api/admin/**`，预期 HTTP 403。
3. 再用管理员 Token 验证正常、参数错误、越权与状态冲突场景。
4. 后端通过后接 Vue 页面；浏览器分别使用学生和管理员身份检查菜单、直接访问 URL 和接口行为。
5. 每小节完成后补回本文件的结果，再开始下一小节。

## 已完成验收

- [x] 管理员 Token 可查看、新增、编辑、启用/停用分类；普通 `USER` Token 请求管理员分类接口返回 403。
- [x] 分类停用后，普通用户使用该分类创建或编辑商品被后端拒绝，提示“分类不存在或已停用”。
- [x] 管理员浏览器可进入分类管理页，并完成新增、编辑、停用、启用分类；每次操作后表格均重新加载。
- [x] 普通学生浏览器没有“管理后台”入口，直接访问 `/admin/categories` 跳转 403。

## 本次用户管理进展（停在这里）

用户管理设计见：[管理员用户管理设计](../superpowers/specs/2026-09-01-admin-user-management-design.md)，实现计划见：[管理员用户管理计划](../superpowers/plans/2026-09-01-admin-user-management.md)。设计和计划已经改为“代码全枚举化、数据库与 API 值不变”：`UserRole` 对应 `USER`/`ADMIN`，`UserStatus` 对应 `ENABLED(1)`/`DISABLED(0)`；`sys_user` 表结构和既有数据不改。

本次由用户手动完成、助手已逐项静态检查的内容：

- [x] `docs/api/phase-6.md` 已补管理员用户分页、筛选、状态更新、权限和错误约定。
- [x] 新增 `user/enums/UserRole.java`、`UserStatus.java`，使用 MyBatis-Plus `@EnumValue` 与 Jackson JSON 转换，保持角色字符串和状态数值的既有接口值。
- [x] `SysUser.role`、`SysUser.status` 改为 `UserRole`、`UserStatus`；注册、登录、`/api/auth/me`、JWT 签发和 JWT 过滤器已同步使用枚举。
- [x] JWT 过滤器对用户不存在、禁用或角色为空的记录不建立认证上下文；正常用户以数据库最新角色建立 `ROLE_<role>` 权限。
- [x] 新增管理员用户查询 DTO、状态更新 DTO、状态查询参数 Converter、脱敏响应 VO，以及 `AdminUserService.listUsers`：支持用户名/邮箱模糊筛选、角色/状态精确筛选、创建时间和 ID 倒序分页，且不返回密码哈希。
- [ ] `AdminUserService.updateUserStatus` 尚未编写：需处理用户不存在（`1005`）、禁止管理员禁用自己（`1006`）和状态更新。
- [ ] `AdminUserController` 尚未创建；列表和状态更新接口仍未暴露。
- [ ] 尚未为非法枚举查询参数补统一 JSON `400` 异常响应；Controller 完成时一并检查现有全局异常处理。
- [ ] 未运行构建、格式化或自动测试，亦未进行 Postman/浏览器验收（按用户既定决定）。

## 下次从这里继续

1. 先在 `AdminUserService` 手动实现 `updateUserStatus(currentAdminId, targetUserId, UserStatus status)`：查询目标用户；不存在抛 `BusinessException(1005, "用户不存在")`；当前管理员试图将自己改为 `DISABLED` 时抛 `BusinessException(1006, "不能禁用当前登录管理员")`；否则更新后返回当前用户记录或空响应（Controller 契约需保持一致）。
2. 新建 `AdminUserController`：`GET /api/admin/users` 使用 `@Valid AdminUserQuery`；`PATCH /api/admin/users/{id}/status` 使用 `@Positive @PathVariable`、`@Valid @RequestBody UserStatusUpdateRequest` 和 `@AuthenticationPrincipal AuthenticatedUser`。权限仍由 `/api/admin/**` 集中限制。
3. 为枚举查询参数转换失败补统一 JSON `400` 处理，并手动检查 `?role=BAD`、`?status=2`、`?status=abc`。
4. 用 Postman 完成管理员分页/筛选、普通用户 `403`、不能禁用自己、禁用后无法登录与旧 Token `401` 的验收；然后再开始前端管理页。

## 收尾标准

- [x] 所有 `/api/admin/**` 接口由后端统一限制为 `ADMIN`，普通用户请求均为 403。
- [x] 本地管理员账号可登录；公开注册仍只能生成 `USER`。
- [x] 管理员分类接口与停用分类后的商品提交限制完成 Postman 验收。
- [x] 管理员分类管理页面完成浏览器验收。
- [x] 学生浏览器的管理员菜单隐藏与直接路由访问 403 完成验收。
- [ ] 分类、用户、商品、订单管理接口完成手动验收。
- [ ] 用户禁用后不能登录，且旧 Token 不能继续请求受保护接口。
- [ ] 管理员前端菜单、路由、四个管理页面完成浏览器验收；学生不可见、直接访问跳 403。
- [ ] 更新最终交接记录；由用户自行暂存并提交 Git。
